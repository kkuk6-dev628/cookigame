
#pragma once
#include "enum.h"

BETTER_ENUM(GoalTypes, char, WaffleObject, PathObject, TopplingObject, PopsicleObject, HiderSegmentObject, HopplingObject, thoppling)

BETTER_ENUM(TileColors, char, blue, purple, yellow, green, orange, red, any, random)

BETTER_ENUM(ModifierTypes, char, NoShuffleModifier)

BETTER_ENUM(SetTypes, char, set)

BETTER_ENUM(HiderSegmentPosition, char, head, tail, body)

BETTER_ENUM(ChainTypes, char, chain1, chain2, chain3)